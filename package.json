{
  "name": "endpiont-app",
  "version": "1.0.0",
  "description": "Roger Sosa",
  "main": "api/index.js",
  "scripts": {
    "start": "node api/index.js",
    "build": "npm run --prefix web build && npm run --prefix api build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "10"
  }
}
